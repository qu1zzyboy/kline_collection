# Binance Futures K线采集服务配置文件
# 复制此文件为 .env 并修改相应的配置值

# Redis 连接 URL
REDIS_URL=redis://127.0.0.1:6379

# 每个品种最多存储的 K 线数量
MAX_KLINES_PER_SYMBOL=600

# Redis Key 前缀
REDIS_KEY_PREFIX=binance:kline:1s

# 日志目录
LOG_DIRECTORY=logs

# 日志级别 (stdout): OFF, TRACE, DEBUG, INFO, WARN, ERROR
LOG_LEVEL_STDOUT=Info

# 日志级别 (file): OFF, TRACE, DEBUG, INFO, WARN, ERROR
LOG_LEVEL_FILE=Info

# Binance 产品类型: UsdM, CoinM
BINANCE_PRODUCT_TYPE=UsdM

# Binance 环境: Mainnet, Testnet
BINANCE_ENVIRONMENT=Mainnet

# Redis 密码（可选，默认为空）
REDIS_PASSWORD=

# Redis 数据库编号（0-15，默认为 0）
# 使用不同的数据库可以隔离不同服务的数据
# 例如：0 用于开发，1 用于生产，2 用于测试
REDIS_DATABASE=0
